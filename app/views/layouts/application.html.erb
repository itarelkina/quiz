<!DOCTYPE html>
<html>
  <head>
    <title>Quiz</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
  <header class="row">
    <div class="col-3">
    <%= link_to root_path, class: 'logo' do %>
    <div class="circle">
    <br />
      Quiz
    </div>
    <% end %>
    </div>
    <div class="col-4 slogan">
      Quiz is about effectively organizing the workflow around opened insident tickets.
    </div>
    <div class="col-5">
    <%= link_to 'Open a ticket', new_ticket_path %>
        </div>
  </header>
  <hr />
  <br class="clear" />
  <div class="container">
  <% if flash[:error].present? %>
    <div class="col-10 offset-1 alert alert-danger">
    <%= flash[:error].html_safe %>
    </div>
  <% end %>
  <%= yield %>
</div>
  </body>
</html>
